import { Component } from "@angular/core";

@Component({
  selector: 'app-header',
  templateUrl: './header.component.html',
  styleUrls: ['./header.component.css'] // Check if the file exists in the specified path and provide the correct path if necessary
})
export class HeaderComponent {}
